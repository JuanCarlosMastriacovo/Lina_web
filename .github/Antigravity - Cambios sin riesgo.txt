Viewed Diagn%C3%B3stico%20de%20calidad%20by%20Antigravity.md:1-361

Veo que ya tenés un diagnóstico de calidad de código. Tu pregunta sobre el PDF de [lina112.py](cci:7://file:///d:/desarrollo_web/lina_web/CapaUI/lina112.py:0:0-0:0) quedó abierta. Pero ahora me preguntás qué puedo modificar sin riesgo. 

Basándome en el diagnóstico, acá te clasifico las correcciones de **menor a mayor riesgo**:

---

## ✅ Sin riesgo (cambios puramente cosméticos/de formato)

| # | Qué | Dónde |
|---|---|---|
| 4.3 | Corregir indentación de 8→4 espacios | `tablebase.py` líneas 18–44 |
| 4.1 | Reemplazar `__import__("datetime")` por `timedelta` en el import | `lina41.py`, `lina43.py` |

---

## 🟢 Bajo riesgo (cambios de 1 línea, sin lógica)

| # | Qué | Dónde |
|---|---|---|
| 3.1 | Cambiar `from CapaDAL.config import APP_CONFIG` → `from CapaBRL.config import APP_CONFIG` | 19 módulos UI |
| 6.4 | Unificar nombres de campos del formulario de login | [lina0.py](cci:7://file:///d:/desarrollo_web/lina_web/CapaUI/lina0.py:0:0-0:0) |
| 6.2 | Cambiar `readonly=False` → `readonly=True` en `row_get()` | `tablebase.py` línea 430 |

---

## 🟡 Riesgo medio (requieren verificación luego)

| # | Qué | Por qué hay riesgo |
|---|---|---|
| 4.5 | Sacar secret key hardcodeada | Rompe sesiones activas al reiniciar |
| 1.4 | Eliminar llamadas redundantes a `set_prog_code()` | Hay que verificar que no haya side-effects |

---

## 🔴 No tocar sin mucho cuidado

- **1.1, 1.2, 1.3**: Refactoring de funciones duplicadas → riesgo de regresiones
- **2.1**: Eliminar `get_table_ui_metadata` de una capa → puede romper importaciones
- **3.2**: Mover `SELECTOR_MAX_ROWS` a DAL → requiere reestructura de imports

---

**¿Querés que empiece por los cambios "sin riesgo" (4.1 y 4.3)?** Son seguros, mejoran la legibilidad, y se pueden revisar en segundos. También puedo hacer el **3.1** (cambiar los imports de `APP_CONFIG`) que es de 1 línea por archivo y muy verificable.